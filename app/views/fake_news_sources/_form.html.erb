<%= form_with(model: fake_news_source, local: true) do |form| %>
  <% if fake_news_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fake_news_source.errors.count, "error") %> prohibited this fake_news_source from being saved:</h2>

      <ul>
      <% fake_news_source.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :query %>
    <%= form.text_field :query %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
