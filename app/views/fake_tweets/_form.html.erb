<%= form_for([@fake_tweet.fake_news_source, @fake_tweet]) do |f| %>
  <% if @fake_tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fake_tweet.errors.count, "error") %> prohibited this fake_tweet from being saved:</h2>

      <ul>
      <% @fake_tweet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
